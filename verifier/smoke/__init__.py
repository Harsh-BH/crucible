"""Smoke-test specs for the Crucible verifier layer.

Empty package marker. Concrete smoke-test specifications (per-task acceptance
checks consumed by the verifier backends) are added by later work.
"""
