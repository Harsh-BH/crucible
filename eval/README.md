# eval (WIP)

Benchmarking and ablation harnesses for Crucible. `benchmark.py` (evaluate a
trained policy on the `infra_synth` environment) and the ablation scripts
(verifier backend, reward shaping, resource limits) land here.

**Status:** scaffolding only — added by a later subagent.
